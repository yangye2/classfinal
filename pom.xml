<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.sonatype.oss</groupId>
        <artifactId>oss-parent</artifactId>
        <version>7</version>
    </parent>

    <groupId>io.github.yangye2</groupId>
    <artifactId>classfinal</artifactId>
    <version>2.0.5</version>
    <modules>
        <module>classfinal-core</module><!--主要代码在这里-->
        <module>classfinal-fatjar</module><!--单独运行的jar、javaagent-->
        <!--        <module>classfinal-web</module>&lt;!&ndash;webUI&ndash;&gt;-->
        <module>classfinal-maven-plugin</module><!--maven插件-->
    </modules>
    <packaging>pom</packaging>


    <name>ClassFinal</name>
    <description>Java class file encryption tool</description>
    <url>https://github.com/yangye2/classfinal</url>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>1.8</java.version>
        <javadocExecutable>${java.home}/bin/javadoc</javadocExecutable>
    </properties>

    <licenses>
        <license>
            <name>The Apache Software License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>
    <scm>
        <url>https://github.com/yangye2/classfinal</url>
        <connection>scm:git:git://github.com/yangye2/classfinal.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/yangye2/classfinal.git</developerConnection>
    </scm>
    <developers>
        <developer>
            <name>yangye2</name>
            <email>yangye2@qq.com</email>
            <organization>yangye2</organization>
            <organizationUrl>https://github.com/yangye2</organizationUrl>
        </developer>
        <developer>
            <name>roseboy</name>
            <email>roseboy@live.com</email>
            <roles>
                <role>Original Author</role>
            </roles>
        </developer>
    </developers>

    <!-- Maven Central 发布说明：
         正式版（非 SNAPSHOT）由 release profile 里的 central-publishing-maven-plugin
         打包上传到 Central Portal，不走这里的 <repository>，所以不再保留
         已退役的 OSSRH 地址（s01.oss.sonatype.org）。
         下面只留 -SNAPSHOT 的仓库地址（Central Portal 快照仓库）。
         注：当前插件版本 0.4.0 不支持直接发 SNAPSHOT；如需发布快照，
         要么把插件升到 >= 0.7.0，要么依赖这里的 distributionManagement。 -->
    <distributionManagement>
        <snapshotRepository>
            <id>central</id>
            <name>Central Portal Snapshots</name>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
        </snapshotRepository>
    </distributionManagement>

    <build>
        <plugins>
            <!-- Source 插件（日常开发和发布都需要） -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.2.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- ============================================================
             发布到自己的 GitHub Packages（Maven 仓库）
             用法： mvn -DskipTests deploy -Pgithub-packages
             仓库地址：https://maven.pkg.github.com/yangye2/classfinal
             需要凭据（~/.m2/settings.xml 或 CI 注入）：
               <server><id>github</id><username>yangye2</username><password>PAT</password></server>
             ============================================================ -->
        <profile>
            <id>github-packages</id>
            <distributionManagement>
                <repository>
                    <id>github</id>
                    <name>GitHub Packages - yangye2/classfinal</name>
                    <url>https://maven.pkg.github.com/yangye2/classfinal</url>
                </repository>
                <snapshotRepository>
                    <id>github</id>
                    <name>GitHub Packages - yangye2/classfinal</name>
                    <url>https://maven.pkg.github.com/yangye2/classfinal</url>
                </snapshotRepository>
            </distributionManagement>
        </profile>

        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <!-- JavaDoc 插件（仅用于发布） -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.4.1</version>
                        <configuration>
                            <encoding>UTF-8</encoding>
                            <charset>UTF-8</charset>
                            <docencoding>UTF-8</docencoding>
                            <doclint>none</doclint>
                            <javadocExecutable>${javadocExecutable}</javadocExecutable>
                        </configuration>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- GPG 签名插件（仅用于发布） -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.0.1</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <passphrase>${env.GPG_PASSPHRASE}</passphrase>
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- 新版 Central Portal 发布插件（仅用于正式版本） -->
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.4.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <tokenAuth>true</tokenAuth>
                            <autoPublish>true</autoPublish>
                            <!-- 只发布非 SNAPSHOT 版本 -->
                            <skipPublishing>${project.version.contains("SNAPSHOT")}</skipPublishing>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>snapshot</id>
            <activation>
                <property>
                    <name>performRelease</name>
                    <value>!true</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <!-- 传统 OSSRH SNAPSHOT 部署 -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <version>3.1.1</version>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
